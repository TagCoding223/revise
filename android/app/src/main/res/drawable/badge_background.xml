<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <!-- Tailwind's blue-100 equivalent for a soft, modern look -->
    <solid android:color="#DBEAFE" />
    <corners android:radius="16dp" />
    <!-- Adding a tiny padding so the text breathes -->
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="2dp"
        android:bottom="2dp" />
</shape>